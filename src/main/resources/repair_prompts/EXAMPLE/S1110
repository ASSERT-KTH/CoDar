### Noncompliant
int x = (y / 2 + 1);   

if (a && ((x+y > 0))) {  // Noncompliant
  //...
}

return ((x + 1));  // Noncompliant
### Compliant
int x = (y / 2 + 1);

if (a && (x+y > 0)) {
  //...
}

return (x + 1);
### Noncompliant
{{BUGGY}}
### Compliant
