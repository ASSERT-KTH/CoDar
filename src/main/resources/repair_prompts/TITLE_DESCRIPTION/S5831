AssertJ configuration should be applied
Why is this an issue? A org.assertj.core.configuration.Configuration will be effective only once you call Configuration.apply() or Configuration.applyAndDisplay(). This rule raises an issue when configurations are set without the appropriate call to apply them. 
### Noncompliant
{{BUGGY}}
### Compliant
