Overrides should match their parent class methods in synchronization
Why is this an issue? When @Overrides of synchronized methods are not themselves synchronized, the result can be improper synchronization as callers rely on the thread-safety promised by the parent class. 
### Noncompliant
{{BUGGY}}
### Compliant
