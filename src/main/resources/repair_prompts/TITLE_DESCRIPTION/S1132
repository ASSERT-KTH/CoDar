Strings literals should be placed on the left side when checking for equality
Why is this an issue? It is preferable to place string literals on the left-hand side of an equals() or equalsIgnoreCase() method call. This prevents null pointer exceptions from being raised, as a string literal can never be null by definition. 
### Noncompliant
{{BUGGY}}
### Compliant
